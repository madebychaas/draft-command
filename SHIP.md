# Ship Checklist

## 1. Create the public repository

Recommended repo:

`madebychaas/draft-command`

Description:

`Real-time adversarial decision intelligence for sequential choice under uncertainty.`

Suggested topics:

`decision-intelligence`, `ai-systems`, `simulation`, `optimization`, `real-time-systems`, `human-in-the-loop`, `fantasy-football`, `python`, `sqlite`

## 2. Publish this package

Keep the private operational repository private.

The public repo should start from this curated showcase package rather than from a sanitized fork of the private repo.

## 3. First public push

Review `docs/PUBLICATION_BOUNDARY.md` before pushing.

## 4. GitHub Pages

The `site/` directory is a ready static one-page concept. It intentionally uses:
- no sidebar
- no redundant navigation
- decision-first hierarchy
- Northstar purple/gold brand colors
- semantic green/amber/red that remain independent of team theme

## 5. Visuals

The files in `assets/concepts/` are design references only. Do not treat them as final production screenshots until their displayed data and media rights have been verified.
